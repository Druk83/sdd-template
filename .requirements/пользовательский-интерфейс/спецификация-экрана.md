# Спецификация экрана (screen specification): {{screen.title}}

Статус: `{{screen.status}}`
Идентификатор экрана: `{{screen.screen_id}}`
Тип экрана: `{{screen.screen_type}}`
Маршрут / точка входа: `{{screen.route_or_entry_point}}`

## Происхождение данных (provenance)

- Ссылки на паттерны (pattern refs): `{{screen.pattern_refs}}`
- Ссылки на шаблоны (template refs): `{{screen.template_refs}}`
- Версия набора шаблонов (template set version): `{{provenance.template_set_version}}`
- Точная ревизия (exact revision): `{{provenance.revision}}`

## Контекст

- Назначение (purpose): `{{screen.purpose}}`
- Контекст использования (usage context): `{{screen.usage_context}}`
- Роли и доступ (roles and access): `{{screen.roles_and_access}}`
- Модальность интерфейса (interface modality): `{{screen.interface_modality}}`

## Данные и композиция

- Источник и форма данных (data source and shape): `{{screen.data_source_and_shape}}`
- Блоки и слоты компонентов (blocks and component slots): `{{screen.blocks_and_slots}}`
- Информационная архитектура (information architecture): `{{screen.information_architecture}}`
- Сопоставление токенов дизайна и компонентов (design tokens and component mapping): `{{screen.design_system_mapping}}`

## Поведение

- Действия (actions): `{{screen.actions}}`
- Состояния (states): `{{screen.states}}`
- Переходы (transitions): `{{screen.transitions}}`
- Валидация и ошибки (validation and errors): `{{screen.validation_and_errors}}`
- Нештатные сценарии (non-happy paths): `{{screen.non_happy_paths}}`

## Области отображения и доступность

- Матрица областей отображения (viewport matrix): `{{screen.viewport_matrix}}`
- Адаптивное поведение (responsive behaviour): `{{screen.responsive_behavior}}`
- Доступность (accessibility): `{{screen.accessibility}}`
- Переполнение и порядок фокуса (overflow and focus order): `{{screen.overflow_and_focus_order}}`

## Ссылки и трассируемость (traceability)

- Эталонные артефакты (reference artifacts): `{{screen.reference_artifacts}}`
- Ссылки на требования (requirement refs): `{{screen.requirement_refs}}`
- Ссылки на сценарии (scenario refs): `{{screen.scenario_refs}}`
- Критерии приёмки (acceptance criteria): `{{screen.acceptance_criteria}}`
- Записи GAP (GAP records): `{{screen.gaps}}`
