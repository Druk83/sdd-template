# Пул экранов (screen pool)

Статус: `{{pool.status}}`

## Происхождение данных (provenance)

- URL источника (source URL): `{{provenance.source_url}}`
- Версия набора шаблонов (template set version): `{{provenance.template_set_version}}`
- Точная ревизия (exact revision): `{{provenance.revision}}`
- Версия контракта/схемы (contract/schema version): `{{provenance.contract_version}}`
- Дата генерации (generated at): `{{provenance.generated_at}}`

## Применимость пользовательского интерфейса (UI applicability)

- Типы интерфейса (interface types): `{{ui.interface_types}}`
- Статус (status): `{{ui.status}}`
- Обоснование решения (decision evidence): `{{ui.decision_evidence}}`

## Экраны

Для каждого экрана укажи обязательные поля:

| Поле | Значение |
|---|---|
| `screen_id` | `{{screen.screen_id}}` |
| `title` | `{{screen.title}}` |
| `screen_type` | `{{screen.screen_type}}` |
| `purpose` | `{{screen.purpose}}` |
| `route` / `entry_point` | `{{screen.route_or_entry_point}}` |
| `user_roles` | `{{screen.user_roles}}` |
| `user_task` | `{{screen.user_task}}` |
| `pattern_candidates` | `{{screen.pattern_candidates}}` |
| `states` | `{{screen.states}}` |
| `transitions` | `{{screen.transitions}}` |
| `viewport_candidates` | `{{screen.viewport_candidates}}` |
| `source_refs` | `{{screen.source_refs}}` |
| `status` | `{{screen.status}}` |

Разработчик может изменить состав и свойства пула до статуса `confirmed`. После
подтверждения изменение требований создаёт impact report и новую ревизию.
