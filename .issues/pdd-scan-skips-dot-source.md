# pdd_scan.py игнорирует .source/ и не видит @todo

**Type:** `type:bug`
**Priority:** `priority:P2`
**Severity:** `severity:S2`
**Status:** `status:needs-info`

## Контекст
- Компонент/сервис: `.tools/pdd/pdd_scan.py`
- Версия/коммит: <указать>
- Окружение: Windows (PowerShell), локальный запуск
- Частота: всегда
- Impact: @todo в `.source/` не попадают в реестр, риск пропуска задач

## Шаги воспроизведения
1. Создать файл `.source/demo.txt` с строкой `# @todo #T-1:1h проверить сканер`
2. Запустить `pdd-scan` или `python .tools/pdd/pdd_scan.py`
3. Открыть `.tasks/pdd/@todoregistry.md`

## Ожидаемое поведение
`@todo` из `.source/` попадают в реестр.

## Фактическое поведение
`.source/` пропускается, так как сканер игнорирует все каталоги, начинающиеся с точки.

## Логи/скриншоты
<если есть>

## Дополнительно
- Workaround: запускать сканер по явному пути или убрать фильтр на dot-dirs
- Связанные задачи: <нет>
- Связанные PR: <нет>
