services:
  kroki:
    image: ${KROKI_IMAGE:-yuzutech/kroki:0.30.1}
    restart: unless-stopped
    environment:
      KROKI_MAX_URI_LENGTH: ${KROKI_MAX_URI_LENGTH:-8192}
    networks:
      - kroki-internal

networks:
  kroki-internal:
    driver: bridge
