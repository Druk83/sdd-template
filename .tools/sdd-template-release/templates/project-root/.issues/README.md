# .issues — проблемы проекта

В каталоге хранятся сообщения о проблемах, дефектах и инцидентах проекта.

```text
.issues/
  <ISSUE-ID>.md
  done/
```

Закрытые проблемы перемещаются в `.issues/done/`. Формат issue определяется
правилами `.agents/sdd-template-X.Y.Z/.manifest/issuesmanifest.md`.
